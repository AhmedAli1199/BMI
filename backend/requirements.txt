fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.4
alembic==1.14.0
pydantic==2.12.5
pydantic-settings==2.6.1
python-dotenv==1.0.1
bcrypt==4.2.1
email-validator==2.2.0
apscheduler==3.10.4
httpx==0.28.1
openai==1.54.4
# Google's current unified Gemini SDK - used only for vision (business
# card / returned copy reading) when VISION_PROVIDER=gemini, see llm.py.
# Installed and its API surface verified against llm.py's calls (Client,
# types.Part.from_bytes, GenerateContentConfig's fields) in this session -
# not yet smoke-tested against a real Gemini API key/photo, since none
# was available here.
google-genai==2.24.0
python-multipart==0.0.12
